# Server Port (defaults to 3000 if not set)
PORT=3000

# Node Environment
NODE_ENV=production

# Frontend API URL configuration (Optional - only needed if deploying React app separately from Express)
# VITE_API_URL=https://your-backend-api.com/api
# VITE_SOCKET_URL=https://your-backend-api.com
